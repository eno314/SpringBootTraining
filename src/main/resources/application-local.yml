spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_boot_training?rewriteBatchedStatements=true
    username: dev
    password: dev
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        # JPAの統計情報を出力する
        generate_statistics: true
